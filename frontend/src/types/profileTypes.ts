export type ProfileTypes = {
  name: string;
  iconUrl: string;
  userId: string;
  bio: string;
  isFollowing: boolean;
  totalMiyabi: number;
  totalPost: number;
  followingCount: number;
  followerCount: number;
};
